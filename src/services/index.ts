/**
 * Client-side service re-exports — calls Express backend APIs.
 */
export {
  api,
  generateScript,
  extractKeywords,
  searchMedia,
  downloadMedia,
  startRender,
  getRenderStatus,
  listProjects,
} from '../utils/api';
