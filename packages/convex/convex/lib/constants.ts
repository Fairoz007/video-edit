export const DEFAULT_TEMPLATE_ID = 'template_premium_longform';

/** Shared workspace user when auth is disabled. */
export const APP_USER_ID = 'anonymous';

export const DEFAULT_USAGE = {
  rendersThisMonth: 0,
  ttsCharsThisMonth: 0,
  storageBytes: 0,
} as const;
